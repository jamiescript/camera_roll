# Camera Roll

Browser based version of the iOS photo gallery.

This was produced as a prototype for a client looking for a way of presenting multiple mugshots within a viewport, before Flexbox and CSS Grid was a thing.
